- correct mean calculation for both averaging steps in forward prop.
- dataset and dataloader for inference where target is not available
- test/train/cross-validation split best practices
- L2 regularization and dropout for MLPs
- embedding for padding in case of pre-trained